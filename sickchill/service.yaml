apiVersion: v1
kind: Service
metadata:
  name: sickchill
  namespace: homemediaserver
  labels:
    app: sickchill
spec:
  ports:
  - port: 8123
    # name: ui
    # targetPort: ui
  selector:
    app: sickchill
